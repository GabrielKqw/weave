---
name: weave-audit
description: Audit an entire repository for removable code, needless dependencies, duplicate helpers, dead flexibility, and custom code replaceable by native platform features. Use when the user asks for a Weave audit, repository simplification audit, or a ranked list of bloat.
---

# Weave Audit

Inspect the whole repository without changing it. Confirm usage before calling code dead.

Return a ranked list with location, evidence, safe deletion or simplification, and the replacement when one is needed. Separate confirmed removals from items that require measurement or product approval.
